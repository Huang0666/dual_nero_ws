# P7 文档

## 作用

定义 P7 的候选增强项，避免在 P5/P6 尚未稳定前提前把复杂调度和动态场景塞回主线。

## 当前阶段定位

P7 不是当前立即实现阶段。  
它更像是在 P5 和 P6 稳定之后，再考虑的增强层。

## 主要内容

- 动态障碍物
- 更复杂的任务分支 / 重试 / 调度
- 如确有必要，再评估引入 MTC / BehaviorTree

## 进入前提

1. `P5-Sim` 和 `P5-Real` 已稳定
2. `P6` 已证明感知结果可稳定接入任务链
3. 当前任务复杂度已经明显超出现有 task engine 的简单实现

## 当前明确不做

- 不在 P5-v1 中提前引入
- 不在 P6 第一版中提前引入
